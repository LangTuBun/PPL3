type A struct{};
                func (a A) add(x, y int) int {
                    return x + y;
                };
                func (a A) add(x, y int) int {
                    return x + y;
                };